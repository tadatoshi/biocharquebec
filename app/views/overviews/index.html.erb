<% if user_signed_in? %>
  <p id="add"><%= link_to t("common.add"), new_overview_path %></p>
<% end %>

<div id="content-list">
  
  <% @overviews.each do |overview| %>
  <div>
    <h3><a href="#"><%= overview.title %></a></h3>
    <p><%= overview.description %></p>
  </div>
  <% end %>
</div>