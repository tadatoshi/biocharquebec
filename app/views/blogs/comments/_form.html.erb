<%= form_for(@comment, :url => blog_post_comments_url(@blog_post)) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit t("common.add") %>
  </div>
<% end %>